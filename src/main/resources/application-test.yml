allowed.origins: '*'
debug: false
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
server:
  port: 9002
spring:
  mvc:
    log-request-details: true

no.nav.security.jwt:
  client:
    registration:
      fpsoknad-mottak:
        token-exchange:
          audience: lokal
      fpoversikt:
        token-exchange:
          audience: lokal
      safselvbetjening:
        token-exchange:
          audience: lokal
  issuer:
    idporten:
      cookie_name: selvbetjening-idtoken

virus:
  enabled: false
fppdfgen:
  enabled: false
mottak:
  uri: ${fpsoknad-mottak.override.url}
oppslag:
  uri: ${fpsoknad-mottak.override.url}
oversikt:
  uri: ${fpoversikt.override.url}
safselvbetjening:
  uri: ${saf.override.url}/
storage:
  passphrase: tullogtøys

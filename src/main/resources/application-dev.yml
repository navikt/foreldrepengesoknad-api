no.nav:
  security.jwt.issuer.selvbetjening:
    discoveryurl: ${loginservice.idporten.discovery.url}
    accepted_audience: ${loginservice.idporten.audience}

management:
    endpoints:
        web:
            exposure:
                include:  '*'
    endpoint:
        health:
            show-details: always

spring:
    mvc:
        log-request-details: true

virus:
  enabled: false
oppslag:
  uri:  https://api-gw-q1.oera.no/fpsoknad-oppslag/api/
  pdl-uri: https://api-gw-q1.oera.no/fpsoknad-mottak/api/pdl/
historikk:
  uri: https://api-gw-q1.oera.no/fpinfo-historikk/api/
mottak:
  uri: https://api-gw-q1.oera.no/fpsoknad-mottak/api/
innsyn:
  mottak: https://api-gw-q1.oera.no/fpsoknad-mottak/api/
  oppslag: https://api-gw-q1.oera.no/fpsoknad-oppslag/api/
minidialog:
  uri: https://api-gw-q1.oera.no/fpinfo-historikk/api/
fppdfgen:
  uri: https://api-gw-q1.oera.no/fppdfgen/

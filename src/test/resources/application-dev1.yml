FORELDREPENGESOKNAD_API_FPSOKNAD_HISTORIKK_API_APIKEY_PASSWORD: foreldrepengesoknad-api+generertKey
FORELDREPENGESOKNAD_API_FPSOKNAD_MOTTAK_API_APIKEY_PASSWORD: foreldrepengesoknad-api+generertKey
FORELDREPENGESOKNAD_API_FPSOKNAD_OPPSLAG_API_APIKEY_PASSWORD: foreldrepengesoknad-api+generertKey
FORELDREPENGESOKNAD_API_S3_CREDS_PASSWORD: tøys
FORELDREPENGESOKNAD_API_S3_CREDS_USERNAME: tull
FORELDREPENGESOKNAD_API_S3_ENDPOINT_URL: some.host.somewhere
FORELDREPENGESOKNAD_API_STORAGE_PASSWORD: tullogtøys
FPSOKNAD_HISTORIKK_API_URL: http://localhost:9002/api
FPSOKNAD_MOTTAK_API_URL: http://localhost:9001/api
FPSOKNAD_OPPSLAG_API_URL: http://localhost:9000/api
TOGGLES_ENABLE_SVANGERSKAPSPENGER: true
TOGGLES_ENABLE_TPSBARN: true
TOGGLES_FNR_HEADER_FILTER: false
http:
  proxy:
management:
  endpoint:
    metrics:
      enabled: true
  endpoints:
    web:
      base-path: /internal/actuator
      expose: '*'
no:
  nav:
    foreldrepenger:
      api:
        allowed:
          origins: http://localhost:8080,http://localhost:8880
        toggles:
          fnr-header-filter: false
      selvbetjening:
        api:
          virus:
            enabled: false
server:
  error:
    whitelabel:
      enabled: false
  port: 8888
  servlet:
    context-path: /
spring:
  main:
    banner-mode: 'off'
  metrics:
    web:
      server:
        auto-time-requests: true
  mvc:
    throw-exception-if-no-handler-found: true
  profiles:
    active: dev
  resources:
    add-mappings: false
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
stub:
  historikk: true
  mottak: true
  oppslag: true

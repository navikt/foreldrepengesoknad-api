name: CodeQl Analyse
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '0 0 * * 0,4'

jobs:
  codeql:
    name: CodeQl
    uses: navikt/fp-gha-workflows/.github/workflows/codeql.yml@main
    secrets: inherit
